{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the pet window is allowed to ask the shell for. Deliberately minimal: it listens for forwarded events and reports its own readiness, and nothing else. Pack loading needs no permission here \u2014 the read access comes from security.assetProtocol.scope in tauri.conf.json, and core:webview/core:app were added alongside it by mistake, granting devtools and webview geometry commands nothing imports.",
  "windows": ["pet"],
  "permissions": [
    "core:default",
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "clipboard-manager:allow-write-text"
  ]
}
